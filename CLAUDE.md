@AGENTS.md

## Claude Code

`.claude/settings*.json` and Claude's own memory are local execution
preferences, not project truth. Everything else this repository asks of an
agent is in the imported file above.
