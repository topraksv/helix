# Graphify already honours .gitignore, so this file only excludes what is
# tracked but is not Helix's own knowledge.
#
# The vendored skill bodies are 224 upstream Markdown files. They would
# outnumber every Helix source file and make community detection describe
# their publishers' prose instead of this codebase. Their authority is
# skills-lock.json, not a derived index.
.agents/skills/
.claude/skills/
