/**
 * Opened from OUTSIDE the Settings tab. The same screen exists at
 * `(tabs)/settings/budgets` for the in-tab route; see `analytics.tsx`
 * for why a cross-tab push belongs at the root rather than behind an anchor.
 */

export { default } from "./(tabs)/settings/budgets";
