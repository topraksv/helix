# Dependencies and logs
node_modules/
*.log

# Local environment and secrets
.env*
!.env.example
*.jks
*.key
*.mobileprovision
*.p8
*.p12
*.pem

# Expo, native and build output
.expo/
/android/
/ios/
.kotlin/
/dist/
/dist-e2e/
/graphify-out/
expo-env.d.ts
*.tsbuildinfo

# Test and local analysis output
/coverage/
/reports/mutation/
/.stryker-tmp/
/playwright-report/
/test-results/
.eslintcache

# Local workbook scratch; committed fixtures under tests/ remain eligible
/*.xlsm
/*.xlsx

# Supabase CLI state
supabase/.temp/

# Obsidian opens `docs/` itself as its vault, so the canonical documents get
# backlinks without a second tree. `app.json` is the shared vault
# configuration; everything else Obsidian writes is per-machine window state.
/docs/.obsidian/*
!/docs/.obsidian/app.json

# Editor and OS debris
.DS_Store
.idea/
.trash/
.vscode/*.local.json
*.orig
*.orig.*
*.rej
*.swp

# Temporary agent workspace (probes, screenshots, intermediate reviews).
# Nothing here is an input to a build or test.
.codex/

# Source-owned AI control plane. Tool settings remain machine-local, while the
# check script and the vendored skills are recoverable from Git.
# Skills are committed rather than restored on demand so that an upstream
# change is reviewable as a diff instead of arriving silently at install time.
/.claude/*
!/.claude/skills/
!/.claude/skills/**
/.ai/*
!/.ai/scripts/
!/.ai/scripts/*.mjs
/.agents/*
!/.agents/skills/
!/.agents/skills/**
