# Copy to .env and fill in from Supabase project settings (Settings → API).
# Only the anon key ships to clients; the service role key must NEVER be here —
# it lives exclusively in GitHub Actions secrets (keep-alive workflow).
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
