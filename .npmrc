# Expo SDK 54's tooling breaks silently on Node 24/26 (native TS stripping).
# Enforce the engines range in package.json instead of documenting it only.
engine-strict=true
# Quarantine newly published packages; npm reports when a security fix needs an
# explicit exception instead of silently accepting a brand-new release.
min-release-age=7
